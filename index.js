const writeCards = ["Ada","Brendan","Ali"];

function writecard(writeCards) {
  console.log("Thank you, ${writecards}, for the wonderful birthday gift!");
  return writeCards
}

writecard(writeCards);

let countdown = 10;

while (countdown > 0) {
  console.log(countdown++);
  
}